version: '3.8'

services:
  docs:
    build:
      context: .
      dockerfile: Dockerfile.docs
    container_name: aicser-docs
    ports:
      - "3005:3005"
    environment:
      - NODE_ENV=production
      - PORT=3005
      - BASE_URL=/
      - CUSTOM_DOMAIN=aicser-docs.dataticon.com
    volumes:
      - ./docusaurus.config.js:/app/docusaurus.config.js:ro
      - ./sidebars.js:/app/sidebars.js:ro
      - ./package.json:/app/package.json:ro
      - ./src:/app/src:ro
      - ./static:/app/static:ro
    networks:
      - aicser-docs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3005/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  docs-dev:
    build:
      context: .
      dockerfile: Dockerfile.docs.dev
    container_name: aicser-docs-dev
    ports:
      - "3006:3005"
    environment:
      - NODE_ENV=development
      - PORT=3005
      - BASE_URL=/
      - DEBUG=true
      - WATCH=true
    volumes:
      - ./docusaurus.config.js:/app/docusaurus.config.js
      - ./sidebars.js:/app/sidebars.js
      - ./package.json:/app/package.json
      - ./package-lock.json:/app/package-lock.json
      - ./src:/app/src
      - ./static:/app/static
      - ./node_modules:/app/node_modules
    networks:
      - aicser-docs
    restart: unless-stopped
    profiles:
      - dev

networks:
  aicser-docs:
    driver: bridge
    name: aicser-docs-network
